NODE_ENV=development
PORT=3001

# MongoDB must be a replica set for multi-document transactions (see docker/).
MONGODB_URI=mongodb://localhost:27017/supershop?replicaSet=rs0

REDIS_URL=redis://localhost:6379

# Comma-separated allow-list for CORS.
CORS_ORIGINS=http://localhost:3000
